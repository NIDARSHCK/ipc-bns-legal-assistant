# IPC-BNS Legal Assistant Frontend

React + Vite frontend for the RAG-based judicial support system.

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5173`.

Run `supabase_setup.sql` in the Supabase SQL Editor before using authentication,
history, or admin views.
